FROM node:20-alpine

WORKDIR /app

# Placeholder: replace with `COPY package*.json .` and `RUN npm ci` once the Next.js app is scaffolded.

EXPOSE 3000

CMD ["sh", "-c", "echo 'Frontend placeholder — run: npx create-next-app@latest .' && tail -f /dev/null"]
